# Build the Vite app and publish `dist/` to GitHub Pages.
# Site URL: https://anurag-mathur18.github.io/TaskSync/
name: Deploy to GitHub Pages

on:
  push:
    branches: [main, dev]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build
        env:
          # Project Pages URL path (repo name). Trailing slash required by Vite.
          VITE_BASE_PATH: /TaskSync/
          VITE_USE_MOCK_API: 'true'
          VITE_API_BASE_URL: /api/v1
        run: npm run build

      # GitHub Pages has no server rewrite. Copying index → 404 lets React Router
      # handle deep links like /TaskSync/tasks/123 on refresh.
      - name: SPA fallback for GitHub Pages
        run: cp dist/index.html dist/404.html

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: dist

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
